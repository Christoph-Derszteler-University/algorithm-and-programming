#pragma once

#include "vector"

/**
  This method sorts the given array in place via the min sort algorithm.

  @param array - The array to be sorted (in place)
  @return - The methods returns no sorted array as the specified array is sorted in place
*/
void minsort_in_place(std::vector<int>& array);